# Choose one environment-specific template instead:
#
#   cp .env.dev.example .env.dev.local
#   cp .env.prod.example .env.prod.local
#
# The environment wrapper intentionally does not load a shared .env.local so
# dev and prod Firebase projects cannot be mixed accidentally.
