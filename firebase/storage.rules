rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Keep Storage private until owner authorization and upload validation exist.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
